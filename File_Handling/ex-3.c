#include <stdio.h>
typedef struct List list
{
    int data;
    list *next;
};

int main(){
    list *head = NULL, *temp = NULL;
    
    int l;
    printf("enter your linkedlist size:");
    scanf("%d",&l);

    for(int =0; i<l; i++)
    {
       list *p = malloc(sizeof(list));
       scanf("%d",&p->data);
       
    }

    while()


   
    return 0;
}